---
import SunIcon from "../icons/Sun.astro";
import MoonIcon from "../icons/Moon.astro";
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import LanguagePicker from "./LanguagePicker.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<header
  class="flex flex-col w-full mx-auto justify-center items-center min-h-20 animate-blurred-fade-in"
>
  <div class="flex justify-between w-full mx-auto items-center min-h-20">
    <nav class="flex flex-row gap-x-2 sm:gap-x-6 font-bold">
      <a
        class="opacity-80 hover:scale-110 hover:opacity-80 transition-all text-xs lg:text-base"
        href="#home">{t("nav.home")}</a
      >

      <a
        class="opacity-80 hover:scale-110 hover:opacity-80 transition-all text-xs lg:text-base"
        href="#skills">{t("nav.skills")}</a
      >
      <a
        class="opacity-80 hover:scale-110 hover:opacity-80 transition-all text-xs lg:text-base"
        href="#projects">{t("nav.projects")}</a
      >
      <a
        class="opacity-80 hover:scale-110 hover:opacity-80 transition-all text-xs lg:text-base"
        href="#contact">{t("nav.contact")}</a
      >
    </nav>

    <div class="flex items-center gap-4">
      <button
        id="theme-toggle-btn"
        class="opacity-80 hover:scale-110 hover:opacity-80 transition-all text-xs lg:text-base"
        aria-label="{t('theme.toggle')}"
      >
        <SunIcon class="hidden dark:block" />
        <MoonIcon class="block dark:hidden" />
      </button>
    </div>
  </div>
  <LanguagePicker />
</header>

<script is:inline>
  const getTheme = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
      return "dark";
    }
    return "light";
  };

  const setTheme = () => {
    const theme = getTheme();
    document.documentElement.className = theme;
    localStorage.setItem("theme", theme);
  };

  setTheme();

  document.addEventListener("astro:page-load", () => {
    const $themeToggleBtn = document.getElementById("theme-toggle-btn");
    setTheme();
    const handleToggleClick = () => {
      const element = document.documentElement;
      element.classList.toggle("dark");

      const isDark = element.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    };

    $themeToggleBtn.addEventListener("click", handleToggleClick);
  });
</script>
